{
  "name": "as-substrate",
  "version": "0.0.1",
  "description": "Collection of projects written in AssemblyScript",
  "author": "Stefanie Doll",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "projects/*"
  ],
  "dependencies": {
    "yarn": "^1.21.1"
  },
  "devDependencies": {
    "@as-pect/cli": "^2.7.0",
    "@types/node": "^10.17.2",
    "assemblyscript": "^0.9.2",
    "typescript": "^3.2.2"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "build:sh": "yarn workspaces ./build.sh",
    "build": "yarn workspaces run build",
    "test": " yarn workspaces run test"
  }
}
