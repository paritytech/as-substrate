{
  "name": "as-substrate",
  "version": "0.0.1",
  "description": "Collection of Substrate projects written in AssemblyScript",
  "author": "Stefanie Doll",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "contracts/*",
    "contracts/raw/*"
  ],
  "dependencies": {
    "yarn": "^1.21.1"
  },
  "devDependencies": {
    "@types/node": "^10.17.2",
    "assemblyscript": "0.12.3",
    "typescript": "^3.2.2"
  },
  "scripts": {
    "clean": "rm -rf node_modules && yarn workspaces run clean",
    "build": "yarn workspaces run build",
    "test": " yarn workspaces run test"
  }
}
